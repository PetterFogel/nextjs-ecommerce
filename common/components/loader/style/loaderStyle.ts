import { makeStyles } from "tss-react/mui";

export const loaderStyles = makeStyles()({
  root: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    height: "20rem"
  }
});
